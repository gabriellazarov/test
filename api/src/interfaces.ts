export interface ProductData {
  id: string;
  appId: string;
  inSubscription: boolean;
  title: string;
  subtitle: string;
  description: string;
  subscription: string;
}

export interface Credentials {
  username: string;
  password: string;
}
